<div dir="rtl">

# 🔍 تقرير Checkout شامل — ميثاق | MITHAQ v2.0

**تاريخ الفحص:** 23 سبتمبر 2026 • **الفاحص:** Buffy (Freebuff) • **الإصدار:** 2.0.0
**نوع الفحص:** مراجعة كود كاملة + فحص أنواع TypeScript + بناء إنتاج فعلي

---

## ✅ نتيجة الفحص الفعلي

| الاختبار | النتيجة |
|---|---|
| تثبيت الحزم (`npm install`) | ✅ 68 حزمة — حلّينا مشكلة IPv6 المعلّقة في الشبكة |
| فحص الأنواع (`tsc --noEmit`) | ✅ **صفر أخطاء** |
| بناء الإنتاج (`next build`) | ✅ **14 مسار** — نجاح كامل (exit 0) |
| Middleware الحماية | ✅ 87.2 kB — يعمل على الحافة |
| تنظيف المشروع | ✅ الجذر Next.js صِرف — القديم كله في `archive/legacy-app/` |

---

## 🗺️ خريطة المسارات المُختبَرة (14)

### صفحات
| المسار | الحماية | الوصف |
|---|---|---|
| `/` | 🔒 جلسة | لوحة العقود (المالك فقط) |
| `/login` | عام | تسجيل الدخول بـ Google |
| `/share/[id]` | 🌐 عام | صفحة الطرف الثاني — **التوقيع عبر الرابط فقط** (لا لوحة على جهاز المالك) |
| `/verify/[id]` | 🌐 عام | التحقق من سلامة العقد بالبصمة SHA-256 |
| `/print/[id]` | 🔒 جلسة | نسخة A4 لحظية للطباعة/PDF (المالك فقط) |

### واجهات API
| المسار | الحماية | الوظيفة |
|---|---|---|
| `POST /api/contracts` | 🔒 جلسة | إنشاء عقد + توليد النص والبصمة وshareToken |
| `GET /api/contracts` | 🔒 جلسة | قائمة عقود المستخدم (معزولة بمالكها) |
| `GET/PUT/DELETE /api/contracts/[id]` | 🔒 جلسة + ملكية | قراءة/تعديل (يُمنع بعد التوقيع الكامل)/حذف |
| `POST /api/contracts/[id]/signatures` | 🔒 مزدوجة | توقيع party1 يتطلب جلسة المالك — party2 عبر الرابط العام فقط |
| `GET /api/contracts/[id]/share-info` | 🔒 جلسة + ملكية | بيانات نافذة الإرسال (رابط + واتساب + بصمة) |
| `GET /api/verify/[id]` | 🌐 عام | تحقق برمجي — يعيد حساب البصمة ويقارنها |
| `GET /api/me` + `PATCH` | 🔒 جلسة | بروفايل المستخدم (هاتف/عنوان/تفضيلات) — **جديد** |
| `GET /api/health` | 🌐 عام | فحص صحة النظام + الاتصال الحي بـ Neon — **جديد** |
| `/api/auth/[...nextauth]` | — | Auth.js v5 (Google) + مزامنة جدول users |

---

## 🟢 ما هو مكتمل في الباكند

- ✅ **قاعدة Neon Postgres عبر Drizzle** — 4 جداول: `users`، `contracts`، `signature_events`، `payment_requests` مع فهارس وقيود مرجعية
- ✅ **عزل ملكية صارم**: كل استعلام تعديل/قراءة خاص يشترط `ownerId = session.dbId` — لا يمكن لأحد رؤية أو تعديل عقود غيره
- ✅ **سلامة الوثيقة**: يُمنع تعديل العقد بعد `signed`، ويُمنع توقيع الطرف نفسه مرتين
- ✅ **سجل إثبات التوقيع**: كل توقيع يُحفظ مع IP + User-Agent + وقت في `signature_events`
- ✅ **بصمة موحّدة** (SHA-256 على 13 حقلاً) تُختم عند الإنشاء/التعديل/التوقيع وتُقارن في `/verify`
- ✅ **توليد نص العقد** مع محرك البنود الديناميكية (placeholders) — منقول ومحسّن من النسخة الأولى
- ✅ **إشعارات تيليجرام** عند فتح/توقيع العقد (اختيارية — لا تُفشل الطلب)
- ✅ **رسالة واتساب الرسمية** + روابط مشاركة من `NEXT_PUBLIC_BASE_URL`
- ✅ **أول مستخدم مشرف تلقائياً** في قاعدة نظيفة (أو عبر `ADMIN_EMAILS`)

---

## 🟠 ما ينقص قبل الإنتاج الكامل (غير حاجب للنشر)

| # | الملاحظة | الحل المقترح |
|---|---|---|
| 1 | لوحة العقود في `/` مؤقتة (بطاقة ترحيب) | بناء Dashboard كامل: جدول + نموذج إنشاء + نافذة إرسال (الخطوة التالية) |
| 2 | لا Rate Limiting على الـ API | إضافة `@upstash/ratelimit` أو middleware بسيط |
| 3 | لا اختبارات آلية | إضافة Vitest لمسارات التوقيع والبصمة (الأهم أمنياً) |
| 4 | بيانات النسخة الأولى (20 عقد JSON) لم تُرحَّل | أداة ترحيل واحدة عند الطلب — العقود القديمة تبقى في الأرشيف |
| 5 | QR مخزّن كـ `qrDataUrl` غير مستخدم بعد | تفعيله في صفحة المشاركة عند بناء اللوحة |

---

## 🚀 حالة النشر على Vercel — جاهز

| العنصر | الحالة |
|---|---|
| `vercel.json` (إقامة `fra1` — الأقرب للشام) | ✅ جاهز |
| Next.js App Router (يُكتشف تلقائياً) | ✅ |
| مسار صحة للمراقبة `/api/health` | ✅ |
| `.gitignore` يشمل `.env*.local` | ✅ |
| دليل النشر خطوة بخطوة | ✅ `README.md` |

### متغيرات البيئة المطلوبة على Vercel
```
DATABASE_URL=          # Neon (pooled connection string للإنتاج)
AUTH_SECRET=           # openssl rand -base64 32
AUTH_GOOGLE_ID=        # OAuth Client ID
AUTH_GOOGLE_SECRET=    # OAuth Client Secret
NEXT_PUBLIC_BASE_URL=https://miithaq.com
ADMIN_EMAILS=          # بريدك
TELEGRAM_BOT_TOKEN=    # اختياري
TELEGRAM_CHAT_ID=      # اختياري
```

### خطوات النشر المختصرة
1. ارفع المشروع إلى GitHub (الجذر = مجلد المشروع مباشرة)
2. Vercel → **Add New Project** → اختر المستودع → Deploy
3. أضف متغيرات البيئة أعلاه (Environment: Production)
4. Settings → Domains → `miithaq.com` + سجلات DNS (CNAME/A من لوحة Vercel)
5. بعد أول نشر: افتح `/api/health` للتأكد من الاتصال بقاعدة Neon ✅

---

## ⚠️ ملاحظة تشغيلية لجهازك

شبكتك تعطّل IPv6 (`ENETUNREACH`) وهذا يعلّق npm إلى الأبد. الحل الدائم:
```bash
setx NODE_OPTIONS "--dns-result-order=ipv4first"
```
ثم أعد فتح الطرفية — يُصلح npm وnode نهائياً على جهازك.

---

*© 2026 ميثاق v2.0 — تقرير فني داخلي • Buffy (Freebuff)*

</div>
